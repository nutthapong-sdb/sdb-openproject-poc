version: '3.8'

services:
  app:
    build: .
    container_name: openproject-poc-app
    ports:
      - "3001:3001"
    volumes:
      - ./projects.db:/app/projects.db
    # Environment variables can be added here if needed in the future
    environment:
      - NODE_ENV=production
    restart: unless-stopped
